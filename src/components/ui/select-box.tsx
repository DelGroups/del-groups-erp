"use client";

/** Gentelella unified select — alias of `Select`. */
export { default as SelectBox, default } from "@/components/ui/select";
export type { SelectProps as SelectBoxProps } from "@/components/ui/select";
